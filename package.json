{
  "name": "Dog Soldiers Resource Page",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "dependencies": {
 
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server",
    "seed": "node seeders/seed.js",
    "dev": "nodemon server",
    "build": "cd client && npm run build"
  }
}
